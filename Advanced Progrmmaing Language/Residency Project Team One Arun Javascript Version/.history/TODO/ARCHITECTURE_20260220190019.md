# TODO App - Multi-User Architecture & Data Flow Documentation

## 🎯 Project Overview

**Advanced Programming Language - Residency Project**

- Multi-user TODO management application with task categorization
- Each user can create, manage, and assign tasks to other users
- Tasks are categorized and tracked with priority levels
- Full CRUD operations with persistent storage

## 📊 Overall Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     PRESENTATION LAYER                          │
│                      (User Interface)                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │  User    │  │ Category │  │ ToDoList │  │ AddTodo  │       │
│  │Selection │  │  Display │  │ (Compact)│  │(Multi)   │       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘       │
│                                                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                     │
│  │ Header   │  │  Stats   │  │  Footer  │                     │
│  │(Gradient)│  │(Single)  │  │  (Info)  │                     │
│  └──────────┘  └──────────┘  └──────────┘                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                            ↓ (Manages)
┌─────────────────────────────────────────────────────────────────┐
│                    BUSINESS LOGIC LAYER                         │
│                   (ToDoContainer Class)                         │
├─────────────────────────────────────────────────────────────────┤
│  - Multi-user state management                                  │
│  - Current user context tracking                                │
│  - Task filtering by assigned user                              │
│  - Orchestrates all components                                  │
│  - Handles CRUD operations                                      │
│  - Communicates with services                                   │
└─────────────────────────────────────────────────────────────────┘
                            ↓ (Uses)
┌─────────────────────────────────────────────────────────────────┐
│                      SERVICE LAYER                              │
│                   (TodoService Class)                           │
├─────────────────────────────────────────────────────────────────┤
│  - Handles all API calls                                        │
│  - Makes HTTP requests to JSON Server                           │
│  - Supports: /todos, /users, /categories endpoints             │
│  - Error handling & data validation                             │
└─────────────────────────────────────────────────────────────────┘
                            ↓ (HTTP REST API)
┌─────────────────────────────────────────────────────────────────┐
│                      SERVER LAYER                               │
│              (JSON Server - Port 3000)                          │
├─────────────────────────────────────────────────────────────────┤
│  - /todos (GET, POST, PATCH, DELETE)                           │
│  - /users (GET - list of application users)                    │
│  - /categories (GET - task categories)                         │
│  - Watches and persists changes to db.json                      │
└─────────────────────────────────────────────────────────────────┘
                            ↓ (File I/O)
┌─────────────────────────────────────────────────────────────────┐
│                      DATA STORAGE                               │
│                     (db.json file)                              │
├─────────────────────────────────────────────────────────────────┤
│  {                                                              │
│    "todos": [                                                   │
│      { "id": 1, "text": "...", "completed": false },           │
│      ...                                                        │
│    ]                                                            │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🚀 Application Start-Up Flow

### Level 1: Initial Load

```
Browser Load (http://127.0.0.1:8080)
         ↓
    ↙─────────────────────────────────────────────────────────┐
    index.html                                                 │
    ├── Loads Bootstrap CSS (CDN)                             │
    ├── Loads Tailwind CSS (CDN)                              │
    ├── Creates <div id="app"></div>                          │
    └── Loads app.js (module script)                          │
         ↓
```

### Level 2: App Initialization (app.js)

```
app.js Execution
   ↓
1. Import ToDoContainer
2. Get DOM element (#app)
3. Create ToDoContainer instance
4. Call initializeApp() [ASYNC]
   ├── Await todoContainer.initialize()
   │   └── Fetch todos from JSON Server
   ├── Render initial UI
   └── Setup event listeners
   ↓
```

### Level 3: ToDoContainer Initialization

```
ToDoContainer.initialize()
   ↓
1. Call this.todoService.getTodos()
   ├── Make GET request to http://localhost:3000/todos
   ├── Parse JSON response
   └── Return todos array
2. Store todos in this.todos
3. Call updateComponents()
   ├── Pass todos to ToDoList component
   └── Pass todos to Stats component
   ↓
```

### Level 4: Service Layer (TodoService.getTodos)

```
TodoService.getTodos()
   ↓
1. Fetch from "http://localhost:3000/todos"
   ├── GET request
   ├── Wait for response
   └── Parse JSON
2. Return array of todo objects:
   [
     { id: 1, text: "Learn JavaScript", completed: false },
     { id: 2, text: "Build a TODO App", completed: true },
     { id: 3, text: "Master Bootstrap & Tailwind", completed: false }
   ]
   ↓
```

### Level 5: JSON Server Response

```
JSON Server (Port 3000)
   ↓
1. Receives GET /todos
2. Reads db.json file
3. Parses JSON content
4. Returns todos array as JSON response
   ↓
```

---

## 🔄 User Action Flow - Add Todo Example

```
USER CLICKS "ADD TASK" BUTTON
          ↓
┌─────────────────────────────────────────────────────────────┐
│ LEVEL 1: UI EVENT HANDLER (app.js)                         │
├─────────────────────────────────────────────────────────────┤
│ addBtn.addEventListener('click', async () => {              │
│   1. Get input value from <input class="add-todo-input">    │
│   2. Validate text is not empty                             │
│   3. Call todoContainer.addTodoItem(text)                   │
│ })                                                          │
└─────────────────────────────────────────────────────────────┘
          ↓
┌─────────────────────────────────────────────────────────────┐
│ LEVEL 2: BUSINESS LOGIC (ToDoContainer)                    │
├─────────────────────────────────────────────────────────────┤
│ async addTodoItem(text)                                     │
│ {                                                           │
│   1. Validate text.trim()                                   │
│   2. Call await this.todoService.addTodo(text)             │
│   3. If successful:                                         │
│      - Push to this.todos array                             │
│      - Call updateComponents()                              │
│      - Update UI                                            │
│ }                                                           │
└─────────────────────────────────────────────────────────────┘
          ↓
┌─────────────────────────────────────────────────────────────┐
│ LEVEL 3: SERVICE LAYER (TodoService)                       │
├─────────────────────────────────────────────────────────────┤
│ async addTodo(text)                                         │
│ {                                                           │
│   1. Create request body:                                   │
│      {                                                      │
│        text: "New Task",                                    │
│        completed: false                                     │
│      }                                                      │
│   2. POST to http://localhost:3000/todos                    │
│   3. Set Content-Type: application/json                     │
│   4. Parse response → new todo object with ID               │
│   5. Return { id: 4, text: "...", completed: false }        │
│ }                                                           │
└─────────────────────────────────────────────────────────────┘
          ↓
┌─────────────────────────────────────────────────────────────┐
│ LEVEL 4: JSON SERVER (Port 3000)                           │
├─────────────────────────────────────────────────────────────┤
│ POST /todos                                                 │
│ {                                                           │
│   1. Receive POST request with body                         │
│   2. Generate new ID (auto-increment)                       │
│   3. Add to todos array in memory                           │
│   4. Write updated array to db.json                         │
│   5. Return created object with ID                          │
│ }                                                           │
└─────────────────────────────────────────────────────────────┘
          ↓
┌─────────────────────────────────────────────────────────────┐
│ LEVEL 5: DATA PERSISTENCE (db.json)                        │
├─────────────────────────────────────────────────────────────┤
│ File updated:                                               │
│ {                                                           │
│   "todos": [                                                │
│     { "id": 1, "text": "Learn JavaScript", ... },           │
│     { "id": 2, "text": "Build a TODO App", ... },           │
│     { "id": 3, "text": "Master Bootstrap & Tailwind", ... },│
│     { "id": 4, "text": "New Task", "completed": false }  ✨ │
│   ]                                                         │
│ }                                                           │
└─────────────────────────────────────────────────────────────┘
          ↓
RESPONSE RETURNS TO LEVEL 3 → LEVEL 2 → LEVEL 1
          ↓
┌─────────────────────────────────────────────────────────────┐
│ UI RE-RENDERS                                               │
├─────────────────────────────────────────────────────────────┤
│ 1. app.innerHTML = todoContainer.render()                   │
│ 2. New todo appears in the list                             │
│ 3. Stats update (Total count +1)                            │
│ 4. Input field clears                                       │
│ 5. setupEventListeners() re-attaches all handlers           │
└─────────────────────────────────────────────────────────────┘
```

---

## 📋 Component Hierarchy & Responsibilities

```
ROOT
  │
  └─ index.html (Entry Point)
      │
      └─ app.js (App Controller)
          │
          └─ ToDoContainer (Main Component)
              │
              ├─ Header Component
              │   └─ Displays: Title + Subtitle
              │
              ├─ ToDoList Component
              │   ├─ Displays list of todos
              │   ├─ Each todo has:
              │   │   ├─ Checkbox (toggle complete)
              │   │   ├─ Todo text
              │   │   ├─ Edit button
              │   │   └─ Delete button
              │   └─ Shows "No tasks yet" if empty
              │
              ├─ AddTodo Component
              │   ├─ Input field for new task
              │   └─ Add button
              │
              ├─ Stats Component
              │   ├─ Total Tasks count
              │   ├─ Completed count
              │   └─ Pending count
              │
              ├─ Footer Component
              │   ├─ Copyright info
              │   └─ Project info
              │
              └─ TodoService (Data Layer)
                  └─ HTTP requests to JSON Server
```

---

## 🔗 Data Flow for Each Operation

### 1. **FETCH/GET - Load All Todos**

```
User visits app → Browser loads index.html
                    ↓
                  app.js
                    ↓
          initializeApp() [async]
                    ↓
        todoContainer.initialize()
                    ↓
        todoService.getTodos()
                    ↓
        fetch("http://localhost:3000/todos")
                    ↓
        JSON Server: Read db.json
                    ↓
        Return todos array
                    ↓
        Store in this.todos
                    ↓
        updateComponents() → render UI
```

### 2. **CREATE/POST - Add New Todo**

```
User types text + clicks "Add"
                    ↓
        Event listener triggers
                    ↓
        todoContainer.addTodoItem(text)
                    ↓
        todoService.addTodo(text)
                    ↓
        fetch(POST, {text, completed: false})
                    ↓
        JSON Server: Generate ID, add to db.json
                    ↓
        Return new todo object with ID
                    ↓
        Push to this.todos array
                    ↓
        updateComponents() → re-render
```

### 3. **UPDATE/PATCH - Edit or Toggle Todo**

```
User clicks Edit/Checkbox
                    ↓
        Event listener triggers
                    ↓
        todoContainer.editTodoItem(id, text)
        OR
        todoContainer.toggleTodoItem(id)
                    ↓
        todoService.updateTodo(id, {updates})
                    ↓
        fetch(PATCH, {text or completed})
                    ↓
        JSON Server: Update db.json entry
                    ↓
        Return updated todo object
                    ↓
        Update local this.todos array
                    ↓
        updateComponents() → re-render
```

### 4. **DELETE/DELETE - Remove Todo**

```
User clicks Delete button
                    ↓
        Event listener triggers
                    ↓
        todoContainer.deleteTodoItem(id)
                    ↓
        todoService.deleteTodo(id)
                    ↓
        fetch(DELETE, /todos/{id})
                    ↓
        JSON Server: Remove from db.json
                    ↓
        Return success
                    ↓
        Filter out from this.todos array
                    ↓
        updateComponents() → re-render
```

---

## 📁 Project Structure

```
TODO/
├── index.html (Entry HTML file)
├── app.js (Main app controller)
├── db.json (Database file - JSON Server)
├── package.json (Dependencies & scripts)
│
├── ToDoContainer/
│   └── ToDoContainer.js (Main orchestrator, 124 lines)
│
├── Header/
│   └── Header.js (Header component)
│
├── Footer/
│   └── Footer.js (Footer component)
│
├── ToDoList/
│   └── ToDoList.js (Display todos component)
│
├── AddTodo/
│   └── AddTodo.js (Add task form component)
│
├── Stats/
│   └── Stats.js (Statistics component)
│
└── Services/
    └── TodoService.js (API service - handles all HTTP calls)
```

---

## 🎯 Key Technologies & Ports

| Component | Technology             | Port | Purpose            |
| --------- | ---------------------- | ---- | ------------------ |
| Frontend  | HTML5 + ES6 JS         | 8080 | User Interface     |
| Server    | JSON Server            | 3000 | REST API           |
| Styling   | Bootstrap 5 + Tailwind | -    | UI Framework       |
| Data      | JSON (db.json)         | -    | Persistent Storage |

---

## 🔐 Data Model

```javascript
// Todo Object Structure
{
  id: Number,           // Unique identifier (auto-generated by JSON Server)
  text: String,         // Task description
  completed: Boolean    // Task status
}

// Example:
{
  "id": 1,
  "text": "Learn JavaScript",
  "completed": false
}
```

---

## 📝 API Endpoints

```
Base URL: http://localhost:3000

GET    /todos              - Fetch all todos
POST   /todos              - Create new todo
PATCH  /todos/{id}         - Update specific todo
DELETE /todos/{id}         - Delete specific todo

Examples:
GET    /todos/1            - Get todo with id 1
PATCH  /todos/1            - Update todo 1 (body: {completed: true})
```

---

## 🎬 Runtime Commands

```bash
# Start JSON Server (Backend) - Port 3000
npm run server

# Start Live Server (Frontend) - Port 8080
npm start

# Development: Start both servers
npm run dev
```

---

## ✨ Features Implemented

1. ✅ **Add Todo** - Create new tasks with POST request
2. ✅ **View Todos** - Display all tasks from database in compact list
3. ✅ **Edit Todo** - Update task text with PATCH request (pre-filled prompt)
4. ✅ **Delete Todo** - Remove tasks with DELETE request
5. ✅ **Toggle Complete** - Mark done/undone with checkbox + PATCH request
6. ✅ **Statistics** - Single display showing Total/Completed/Pending counts
7. ✅ **Persistence** - Data saved to db.json file
8. ✅ **Responsive UI** - Bootstrap 5.3.8 + Tailwind CSS 4.2.0 styling
9. ✅ **Optimized Typography** - Reduced font sizes for better visual hierarchy
10. ✅ **Gradient Design** - Purple/blue gradient backgrounds for modern look

---

## 🎨 UI/UX Design Details

### Color Scheme

- **Primary Gradient:** `#667eea` (Blue) → `#764ba2` (Purple)
- **Success Color:** `#28a745` (Green for completed)
- **Warning Color:** `#ffc107` (Yellow for pending)
- **Background:** White cards with light gray (#f8f9fa) containers

### Typography (Optimized)

- **Header Title:** `1.2rem` (from Bootstrap display-4)
- **Section Titles:** `0.95rem` (from lead/fs-5)
- **Body Text:** `0.95rem` (reduced from 1rem)
- **Buttons:** `0.8rem` (compact size)
- **Stats Labels:** `0.75rem` (small, secondary text)

### Component Styling

- **Header:** Gradient background with centered title and task count
- **ToDoList:** Card layout with scrollable list (min 300px, max 500px height)
- **AddTodo:** Compact form with gradient header
- **Stats:** Three-card horizontal layout (Total/Completed/Pending)
- **Footer:** Light background with border divider

### Spacing

- **Padding:** 1-1.5rem for major sections
- **Margin:** 0.5rem between items
- **Card Spacing:** 8px between components
- **List Item Margin:** 0.5rem between todos

---

## 🏗️ Architecture Patterns Used

1. **MVC Pattern** - Separation of concerns
   - Model: TodoService (data layer)
   - View: Components (Header, ToDoList, AddTodo, Stats, Footer)
   - Controller: ToDoContainer (business logic orchestrator)

2. **Service Layer Pattern** - TodoService abstracts all API calls

3. **Component-Based Architecture** - Modular, reusable components with render() methods

4. **Async/Await** - Clean asynchronous operation handling

5. **REST API** - Standard HTTP methods for CRUD operations

6. **Event Delegation** - Central event handling in app.js with re-attachment after DOM updates
